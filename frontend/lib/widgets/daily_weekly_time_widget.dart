import 'package:flutter/material.dart';

class DailyWeeklyTimeWidget extends StatelessWidget {
  const DailyWeeklyTimeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("DailyWeekly")
      ],
    );
  }
}