import 'package:flutter/material.dart';

class IntervalTimeWidget extends StatelessWidget {
  const IntervalTimeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Interval")
      ],
    );
  }
}